# Session 2026-07-12 — Boot controller léger (Phases 3+4) + plan robustesse pairing

Branche : `feature/controller-light-boot` (non mergée). Plan complet approuvé (6 PRs) :
`~/.claude/plans/elegant-riding-zebra.md` (copie de référence hors repo).

## Livré

- **PR 1 (`698181f`) — Pairing overlay → DOM (Phase 3).** `PairingOverlay` shell (sheet
  receiver slide-up + plein écran controller en fondu) + `QrCard` (matrice `qrcode`, rendu
  SVG un seul `<path>`, net à tout DPR). `pairingHost` découplé du render worker : callback
  `onPhase(PairingStatus)` (`src/input/signaling/types.ts`), câblé par AppHost vers le shell
  ET le prompt du title (`setControllerPaired`). Supprimés : `PairingOverlayScreen`,
  `PairingPanel`, `QR.ts` — `qrcode` sort du chunk render worker (vérifié au build).
- **PR 2 (`0dd22f9`) — Entry controller pur (Phase 4).** `main.ts` résout le rôle AVANT de
  choisir le chunk (`?controller`/`?receiver` > `?r=` implicite > rôle persisté ambigu) :
  controller → `ControllerHost` (guards, SW, orchestrateur, ShellHost, pairing join immédiat,
  gate d'orientation — AUCUN canvas/render/sim/SAB/benchmark), sinon `AppHost` (ctx injecté,
  branches controller retirées).
- **Mesure (build prod, `vite preview`)** : `/?r=CODE` = 7 requêtes, zéro
  `render.worker|simulation.worker|SystemHost|Havok` (~4 Mo évités). Chunks : index 3,7 kB +
  ControllerHost 0,75 kB + chunk partagé shell/pairing 138 kB (trystero+qrcode+xstate).

## Pièges rencontrés (pour le prochain agent)

- **ScreenManager rejoue le reveal** sur `transition()` vers l'écran déjà courant (le early
  return `to === _current` ne couvre que la sortie d'overlay). D'où le dedupe
  `lastSentScreen` + filtre `PAIRING_MODE` dans AppHost **et** dans `_dev/inspectors/monolith.ts`
  — sans ça, fermer la sheet de pairing rejouait le title.
- **START shell transparaissait** à travers le panneau de pairing (opacité 0,97) →
  `ShellHost` masque START quand `PAIRING_MODE` est ouvert.
- Vite dev tourne en **HTTPS cert auto-signé** (scripts navigateur : `ignoreHTTPSErrors`).

## Vérifié / non vérifié

- ✅ Build + lint + Playwright ; smoke des 2 rôles (dev + build prod) ; cycle sheet
  open/close sans replay ; régressions `?dev`, `?monolith`.
- ❌ **Pairing 2 devices réels** (scan caméra, nostr, TURN cross-réseau) — invérifiable en
  sandbox, à faire avant merge.

## Livré (suite, même journée)

- **Fix QR (`b7c1e79`)** — le QR semblait « passer sous le fond du panneau » : le QR masqué
  (peer découvert) gardait sa boîte de ~245px (opacité 0), et le title canvas transparaissait
  à travers le fond 0,97 dans ce trou. Sheet opaque + QR masqué en `display:none` comme l'URL.
- **PR 3 (`831b972`)** — boot-splash statique (`boot-splash.html` via `@include`, retiré par
  chaque host au 1er écran monté, y compris monolith) ; `platform/idle.ts` ; sim différée
  title-first (façade `simControl` paresseuse, init en idle après `startUp()`) ; prefetch
  idle post-`paired` côté controller (`?worker&url`/`?url` — strings only) ; `PairingChannel`
  en `import()` dynamique dans `join()`. Bundle : trystero isolé (60 kB async), chunk partagé
  shell 138 → 78 kB. Vérifié : `?r=` 8 requêtes zéro chunk lourd ; receiver sim/Havok APRÈS
  title ; START GAME `?dev` jouable ; script inline survit à la minification.

## PR 4 livré (2026-07-13, 2 commits)

- **Commit A (`2c3190c`)** — `connectionMachine.ts` (xstate : idle→joining→searching⇄handshaking→
  paired + `error` ; timers `after` découverte 30 s / handshake 10 s ×3 / auto-rejoin 30 s) ;
  `loopbackChannel.ts` (fake mémoire, zéro réseau) ; protocole `busy` + `protocolVersion` dans
  `PairingChannel` (fix surnuméraires + mismatch diagnosticable) ; garde `onStart` par peer ;
  `pairingHost` réécrit en pur câblage canal↔machine↔shell. Ajout vitest + tests FSM/loopback.
- **Commit B (`61a5700`)** — overlay `error` (message par cause) / `reconnecting` + boutons RETRY
  (même room) et SCAN AGAIN (controller, scanner lazy) ; ICE TTL 30 min + `AbortSignal.timeout(4000)` ;
  room code persistant par onglet (`persistentRoomCode`, sessionStorage). Tests jsdom overlay + identity.
- **31 tests vitest.** Vérifié en build prod (`vite preview`) : `/?r=` reste léger (0 requête
  render/sim/Havok), bascule `searching→error` après 30 s → RETRY/SCAN AGAIN, RETRY repart en searching.
- `reconnecting` : état + type présents mais lifecycle NON câblé (visibilité/wakeLock = PR 5).

## Reste à faire (PRs 5-6 du plan)

5. Ping/pong + phase `reconnecting` + `connectionLifecycle.ts` (visibilitychange/pageshow/
   wakeLock) partagé ControllerHost ET AppHost ; autorité receiver=ACTIVE au re-pair.
6. Docs (CLAUDE.md §5/§9-10, invariant « AppState = écrans du receiver ») + garde CI
   Playwright permanente : aucune requête lourde sur `?r=` (seule protection structurelle
   contre une régression de légèreté par import innocent).
