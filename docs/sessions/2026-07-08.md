# 2026-07-08 — Réorganisation de la documentation

**Fait :**
- Restructuré `docs/` (18 fichiers à plat) en 4 dossiers par nature : `architecture/`
  (référence stable), `design/`, `decisions/` (write-once, datées), `progress/`
  (journal vivant). Créé `docs/README.md` comme index, mis à jour toutes les
  références croisées dans le code et les docs, ajusté CLAUDE.md §17. Commit `9e4375f`.
- Exploré les options d'outillage (Dash, TypeDoc, Notion, Anytype, Craft, Logseq,
  Obsidian, VitePress) pour la doc de code et le suivi de projet.
- Décision : **Logseq** pour le journal libre au fil de la pensée (thème custom
  "verre dépoli" appliqué dans `docs/logseq/custom.css` — probablement à
  recréer, le dossier a disparu suite à un changement de config du graph).
  **Obsidian** pour le board de tâches (`docs/board.md`, plugin Kanban) et la vue
  git (plugin Obsidian Git). Notion et Anytype écartés : format propriétaire,
  je ne peux pas les lire directement.
- Créé `docs/notes/` (personnel, jamais touché par moi sauf demande explicite)
  et `docs/sessions/` (ce dossier — mon journal de session).
- Piste retenue d'une lecture NotebookLM externe : ajouter au board "CI nightly
  build" + "Playwright golden tests" (dette déjà connue, `docs/onboarding.md`
  Annexe C) ; adopter le triage Must/Could pour les colonnes du board.

**Fait (suite) :**
- Utilisateur a installé lui-même : plugins Obsidian **Kanban**, **Obsidian Git**,
  **Style Settings**, **Calendar**, **Homepage**, et un thème téléchargé
  (**Cupertino**, `cssTheme` dans `appearance.json`) — j'ai retiré mon propre
  thème custom (`atelier.css` + réglages `accentColor`/`theme`/snippet) sur sa
  demande pour ne pas interférer. Il a ensuite lui-même activé `translucency:
  true` (vibrancy macOS native, Settings → Appearance → "Translucent window")
  et ajusté police/taille (`New Athena Unicode`, `baseFontSize: 16`).
- Branché le plugin **Calendar** sur mes comptes-rendus : `docs/.obsidian/daily-notes.json`
  pointe le core plugin Daily Notes sur le dossier `sessions/` (format `YYYY-MM-DD`)
  avec un template (`docs/templates/session-template.md`, sections Fait / Pièges /
  Prochaine étape) — cliquer une date dans le calendrier ouvre/crée directement
  le compte-rendu du jour.
- Créé `docs/board.md` (Kanban : Idées/Could, À faire/Must, En cours, Fait),
  déjà rempli avec l'état réel du projet + les cartes CI nightly + Playwright
  golden tests remontées par une lecture NotebookLM externe.
- Discuté un workflow d'auto-résumé de session **via un LLM local (Ollama)**
  plutôt que Claude, pour ne pas consommer de tokens sur une tâche mécanique.
  Modèle retenu : **qwen2.5:7b** (meilleur en français et en suivi d'instructions
  que llama3.2:3b ou le `deepseek-coder:latest` déjà installé, qui est un modèle
  de code peu adapté à de la prose). L'utilisateur est en train de le
  télécharger (`ollama pull qwen2.5:7b`).

**Pièges pour le prochain agent/session :**
- `docs/journals/` et `docs/pages/` (vides) ont été créés par Logseq directement
  à la racine de `docs/` — à surveiller pour ne pas polluer la structure
  architecture/design/decisions/progress qu'on vient de poser.
- `docs/logseq/custom.css` (thème "verre dépoli") a été écrit puis a disparu —
  le dossier `docs/logseq/` n'existe plus. Ne pas le recréer sans demande
  explicite : l'utilisateur a exprimé une préférence claire pour choisir ses
  thèmes lui-même (cf. retrait du thème Obsidian custom sur simple demande).
- Le hook `SessionEnd` (résumé automatique via Ollama) n'est **pas encore
  configuré** — bloqué à dessein par le mode auto tant que l'utilisateur n'a
  pas donné une confirmation explicite et nommée ("configure le hook
  SessionEnd"). Le plan validé avec lui : script `.claude/hooks/session-summary.sh`
  qui (1) dump le JSON reçu par le hook dans un fichier de debug pour vérifier
  le format réel de `transcript_path` avant de coder la suite (ne pas supposer
  le format à l'aveugle), (2) extrait la transcription, (3) l'envoie à
  `ollama run qwen2.5:7b` avec le template de `docs/templates/session-template.md`,
  (4) écrit le résultat dans `docs/sessions/<date>.md`. Déclaration à ajouter
  dans `.claude/settings.json` sous `hooks.SessionEnd`.

**Prochaine étape suggérée :**
1. Confirmer que `ollama pull qwen2.5:7b` est terminé.
2. Obtenir la confirmation explicite de l'utilisateur, puis écrire
   `.claude/hooks/session-summary.sh` + déclarer le hook dans `.claude/settings.json`,
   en commençant par l'étape de debug du payload avant de brancher Ollama.
3. Une fois le hook validé, reprendre le tri de `IDEAS.md` en pages liées
   (`docs/pages/`), resté en attente depuis le début de la session.
