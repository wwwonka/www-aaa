# Dependencies
node_modules/

# Build
dist/

# Animations : seuls les .json (natifs Theatre.js) sont committés — le .anim binaire est un
# artefact généré à `pnpm build` (voir _dev/vite-anim-plugin.ts), jamais dans le dépôt
public/game/animations/*.anim

# Dev certificates — clés privées, ne jamais committer
_dev/.mkcert/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Thumbnails
Icon?

# Dossier de config de volume
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Google Drive
.tmp.driveupload
